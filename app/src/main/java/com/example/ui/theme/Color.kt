package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Palette NetWard : Sécurité Réseau & Audit
val PrimaryNavy = Color(0xFF0F172A)       // Fond sombre / En-tête Slate 900
val AccentBlue = Color(0xFF0284C7)        // Bleu électrique réseau
val AccentCyan = Color(0xFF06B6D4)        // Cyan vif pour les paquets actifs

val SafeGreen = Color(0xFF10B981)         // Équipement fiable / Réseau sain
val SafeGreenContainer = Color(0xFFD1FAE5)

val WarningAmber = Color(0xFFF59E0B)      // Nouvel appareil non vérifié
val WarningAmberContainer = Color(0xFFFEF3C7)

val AlertRed = Color(0xFFEF4444)          // Caméra suspecte / Alerte intrusion
val AlertRedContainer = Color(0xFFFEE2E2)

val SurfaceCardLight = Color(0xFFF8FAFC)  // Cartes claires Slate 50
val SurfaceBorderLight = Color(0xFFE2E8F0)
